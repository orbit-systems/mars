
// copy (size) bytes from (src) to (dest). does not return
let memcopy            = fn #always_inline (dest, src: addr, size: uint)
let memcopy_no_overlap = fn #always_inline (dest, src: addr, size: uint)

let memset
let memzero

// overflow checked operations (where * is any integer type)
let add_overflow_* = fn #always_inline (a, b: *) -> (result: *, overflow: bool)
let sub_underflow_* = fn #always_inline (a, b: *) -> (result: *, underflow: bool)

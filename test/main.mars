module test;

let main = fn() {
    mut arr = [6]int {4, 6, 5, 1, 3, 2};
    quicksort(arr[:]);
    // arr == {1, 2, 3, 4, 5, 6}
};


let x = fn() {...}

        test.x:
            d64 test.sym20384902387402

        test.sym20384902387402:
            {...}
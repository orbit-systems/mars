def Ptr = builtin::types::Pointer;

pub interface Allocator {
    pub enum Err {
        OutOfMemory
    }

    pub fun alloc(^self, def P: Ptr, count: usize): Err?P;
    pub fun realloc[P: Ptr](^self, old: P, new_count: usize): Err?P;
    pub fun free[P: Ptr](^self, ptr: P);
}
